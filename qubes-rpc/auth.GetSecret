#!/bin/sh

pam_secret s=check-auth >/dev/null 2>/dev/null || (zenity --password --title="Unlock user secret" --timeout=30 2>/dev/null | pam_secret auth)

pam_secret s="request" s="qvm:$QREXEC_REMOTE_DOMAIN" s="$QREXEC_SERVICE_ARGUMENT"
